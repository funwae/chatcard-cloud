{
  "name": "chatcard-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @chatcard/web dev:3002",
    "dev:api": "pnpm --filter @chatcard/api dev",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @chatcard/web build",
    "build:api": "pnpm --filter @chatcard/api build",
    "build:proof": "pnpm --filter @chatcard/proof build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
